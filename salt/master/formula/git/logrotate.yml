parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            logrotate:
              source: git
              address: '${_param:salt_master_environment_repository}/logrotate.git'
              revision: ${_param:salt_master_environment_revision}
