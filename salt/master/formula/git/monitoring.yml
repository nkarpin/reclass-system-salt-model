parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            fluentd:
              source: git
              address: '${_param:salt_master_environment_repository}/fluentd.git'
              revision: ${_param:salt_master_environment_revision}
            prometheus:
              source: git
              address: '${_param:salt_master_environment_repository}/prometheus.git'
              revision: ${_param:salt_master_environment_revision}
            telegraf:
              source: git
              address: '${_param:salt_master_environment_repository}/telegraf.git'
              revision: ${_param:salt_master_environment_revision}
