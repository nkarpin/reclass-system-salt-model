parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            manila:
              source: git
              address: '${_param:salt_master_environment_repository}/manila.git'
              revision: ${_param:salt_master_environment_revision}
