parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            dogtag:
              source: git
              address: '${_param:salt_master_environment_repository}/dogtag.git'
              revision: ${_param:salt_master_environment_revision}
