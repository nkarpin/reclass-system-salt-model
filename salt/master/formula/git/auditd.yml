parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            auditd:
              source: git
              address: '${_param:salt_master_environment_repository}/auditd.git'
              revision: ${_param:salt_master_environment_revision}
