parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            kubernetes:
              source: git
              address: '${_param:salt_master_environment_repository}/kubernetes.git'
              revision: ${_param:salt_master_environment_revision}
            etcd:
              source: git
              address: '${_param:salt_master_environment_repository}/etcd.git'
              revision: ${_param:salt_master_environment_revision}
            docker:
              source: git
              address: '${_param:salt_master_environment_repository}/docker.git'
              revision: ${_param:salt_master_environment_revision}
