parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            gnocchi:
              source: git
              address: '${_param:salt_master_environment_repository}/gnocchi.git'
              revision: ${_param:salt_master_environment_revision}
