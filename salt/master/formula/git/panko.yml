parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            panko:
              source: git
              address: '${_param:salt_master_environment_repository}/panko.git'
              revision: ${_param:salt_master_environment_revision}
