parameters:
  salt:
    master:
      environment:
        dev:
          formula:
            ccp:
              source: git
              address: '${_param:salt_master_environment_repository}/ccp.git'
              revision: ${_param:salt_master_environment_revision}
